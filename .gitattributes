# Normalize line endings
* text=auto

# Shell scripts must use LF — they run on Linux (CI agents, Claude hooks)
*.sh text eol=lf

# YAML must use LF for Azure Pipelines compatibility
*.yml text eol=lf
*.yaml text eol=lf
